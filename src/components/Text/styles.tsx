import { StyleSheet } from 'react-native';
import { Colors, FontFamily, FontSize } from '../../styles/vars';

export default StyleSheet.create({
  base: {
    fontFamily: FontFamily.regular,
    color: Colors.text,
    fontSize: FontSize.md,
  },
});
