import { StyleSheet } from 'react-native';
import { Colors } from '../../styles/vars';

export default StyleSheet.create({
  container: {
    flex: 1,
  },
  errorText:{
    color: Colors.error,
  },
});
